---
kind: pipeline
type: docker
name: linux

steps:
- name: build
  image: jojomi/hugo
  commands:
  - hugo
- name: deploy
  image: eeacms/rsync
  environment:
    SSH_KEY:
      from_secret: key
  commands:
  - ./download_ssi.sh
  - python3 process-municipalities.py
  - python3 process-rt.py
  - python3 process-time-series.py
  - echo "Hej"
  # - mkdir -p ~/.ssh
  # - eval `ssh-agent -s`
  # - echo "$SSH_KEY" | ssh-add -
  # - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
  # - rsync --delete -ra -e "ssh -p 8000" public/ ci@barzini.svendcs.com:/var/www/judo.svendcs.com/html
  when:
    branch:
    - master
